apiVersion: v1
kind: ConfigMap
metadata:
  name: django-config
  namespace: maquinarias
data:
  DEBUG: "False"
  ALLOWED_HOSTS: "*"
  DB_NAME: "maquinariasdb"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DJANGO_SUPERUSER_USERNAME: "admin"
  DJANGO_SUPERUSER_EMAIL: "admin@miapp.com"
